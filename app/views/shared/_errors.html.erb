<% if obj.errors.any? %>
  <div class="row"></div>
    <div class="alert alert-danger col-md-8 col-md-offest-2">
      <h5>Please correct the folowing errors:</h5>
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
         <% end %>
      </ul>
    </div>  
<% end %>